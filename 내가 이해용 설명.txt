PredictIn

사용자가 보내는 JSON의 형태를 정의한 입력 스키마야.

예: {"text": "오늘 너무 지치고 힘들다."}

tokenizer(...)

문장을 모델이 이해할 수 있는 숫자 토큰으로 바꿔주는 전처리기

max_length=256은 너무 긴 문장 잘라내서 비용/속도를 통제

torch.no_grad()

학습이 아니라 추론만 하는 모드

메모리/속도에 유리

logits → softmax → probs

logits: 모델이 낸 “원시 점수”

softmax: 이를 확률로 변환

pred_id: 확률이 가장 높은 클래스(0/1)

LABEL_MAP

모델이 0/1로 내는 걸 사람이 읽기 좋은 NEGATIVE/POSITIVE로 바꿔줌

latency_ms

요청 들어온 뒤 응답까지 걸린 시간

운영/성능 지표로 README에 넣기 아주 좋음

print({...})

CloudWatch 로그로 남음

“원문 텍스트를 안 남기고 길이/결과만 남긴다”는 점은 개인정보 고려 포인트라서 포트폴리오에서 강점